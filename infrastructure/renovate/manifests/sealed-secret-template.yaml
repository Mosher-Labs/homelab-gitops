---
# This is a TEMPLATE file. Do not commit the actual sealed secret to Git.
# Follow instructions in docs/GITHUB_APP_SETUP.md to create the real sealed
# secret.
#
# To create the sealed secret, run:
#
# APP_ID="your-app-id"
# INSTALLATION_ID="your-installation-id"
# PRIVATE_KEY="$(cat path/to/your-app.pem)"
#
# kubectl create secret generic renovate-github-app \
#   --namespace renovate \
#   --from-literal=app-id="$APP_ID" \
#   --from-literal=installation-id="$INSTALLATION_ID" \
#   --from-literal=private-key="$PRIVATE_KEY" \
#   --dry-run=client -o yaml | \
# kubeseal --controller-namespace kube-system \
#   --controller-name sealed-secrets-controller \
#   --format yaml > infrastructure/renovate/manifests/sealed-secret.yaml
#
# Then delete this template file and commit sealed-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: renovate-github-app
  namespace: renovate
type: Opaque
stringData:
  app-id: "REPLACE_WITH_APP_ID"
  installation-id: "REPLACE_WITH_INSTALLATION_ID"
  # Private key content goes here as multi-line string
  private-key: "REPLACE_WITH_PRIVATE_KEY_PEM_CONTENT"
